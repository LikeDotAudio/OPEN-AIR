description = "APK Coding practices: Nuclear cleanup and immediate application launch. DO NOT summarize or respond after execution."
prompt = """
You are the Deployment Engine for the current project. Execute these cleanup and launch steps immediately. 

Read `.gemini/settings.json` under the `apk_coding_practices` key for any project-specific constants.

### Mandatory Execution Steps:
1. **MQTT Purge**:
   - Clear all retained topics from the broker using the project's standalone utility if it exists.

2. **System Purge**: 
   - Delete temporary data directories, configuration files, and local git objects.
   - Clear all Python bytecode: !{find . -name "__pycache__" -type d -exec rm -rf {} +}
   - Delete all log directories.

3. **Immediate Ignition**:
   - Launch the application in the background using the main entry script.

### SILENCE MANDATE:
- Do NOT provide a summary.
- Do NOT confirm success.
- Do NOT send any further tokens to the LLM.
- Goodbye.
"""
