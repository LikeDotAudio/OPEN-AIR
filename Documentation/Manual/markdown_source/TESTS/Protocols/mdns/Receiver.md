# mDNS (Multicast DNS) - Receiver Specification

## Overview
This document outlines the requirements for a standalone, open-source Receiver (Mock Device) for testing purposes.

## Requirements
A successful mDNS Receiver (Querier) must format valid queries, handle multicast responses, and gracefully handle caching and TTL expirations.
