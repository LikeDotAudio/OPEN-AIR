# Ember+ (Lawo/L-S-B) - Receiver Specification

## Overview
This document outlines the requirements for a standalone, open-source Receiver (Mock Device) for testing purposes.

## Requirements
A successful Ember+ Receiver (Provider) must serve an Ember+ tree, correctly format BER-encoded Glow responses, notify consumers of value changes asynchronously, and maintain state.
