[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "oamidimapper_rs"
version = "0.1.0"
description = "High-performance MIDI topic mapping for OPEN-AIR"
requires-python = ">=3.7"
dependencies = []
