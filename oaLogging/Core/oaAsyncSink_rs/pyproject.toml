[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "oaasyncsink_rs"
version = "0.1.0"
description = "Native Rust asynchronous log sink for OPEN-AIR."
readme = "README.md"
requires-python = ">=3.12"
authors = [{name = "Gemini Iron Oxide Architect"}]
dependencies = []

[tool.maturin]
features = ["pyo3/extension-module"]
module-name = "oaasyncsink_rs.oaasyncsink_rs"
