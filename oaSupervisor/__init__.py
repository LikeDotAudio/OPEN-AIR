# oaSupervisor — supervisor-process helpers extracted from openair.py.
# Layout follows the standard project subfolders:
#   Methods/  — pure functions, no side effects beyond what they return.
#   Workers/  — process lifecycle (spawn / monitor / shutdown).
